// Storage keys
export const STORAGE_KEYS = {
  ENABLED: 'enabled'
} as const;

// Default settings
export const DEFAULT_SETTINGS = {
  ENABLED: true
} as const;

// Response keys
export const RESPONSE_KEYS = {
  ENABLED: 'enabled',
  SUCCESS: 'success'
} as const;